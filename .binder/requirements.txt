# Binder image dependencies.
#
# The CDS package itself is installed by .binder/postBuild (editable, from the
# repo checkout). This file only lists the extras the demo notebooks pull in at
# runtime that are NOT already declared in pyproject.toml's core dependencies:
#   - matplotlib : the "Tour of Numerical Methods" notebooks render plots inline
#
# Keep intentionally minimal — Binder memory/time is constrained, and CDS's
# pitch is "no heavy dependencies". This list must stay short.
matplotlib>=3.7
